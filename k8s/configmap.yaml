apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-config
  namespace: demo
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  # Configuración de feature flags por defecto
  FEATURE_FLAGS_ENABLED: "true"
